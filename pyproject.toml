[project]
name = "disruption-responder"
version = "0.1.0"
description = "Tiny multi-agent supply-chain disruption responder."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "black>=26.3.1",
    "isort>=8.0.1",
    "langchain>=1.2.17",
    "langchain-openai>=1.2.1",
    "pydantic>=2.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
disruption-responder = "disruption_responder.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["disruption_responder"]
